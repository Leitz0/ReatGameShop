import { useContext } from "react";
import Context from "../context/Context";

export default function Login() {
  return (
    <div>
      <h1>Login</h1>
      <button>Login</button>
    </div>
  );
}
